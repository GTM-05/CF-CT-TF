# Inventory collectors
#
# File collector: inventory/fixtures (tests) and inventory/generated (live dumps).
# AWS Organizations collector: valeotf inventory accounts --live (read-only).
# Do not write to AWS from collectors.
